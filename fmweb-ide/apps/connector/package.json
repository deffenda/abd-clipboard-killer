{
  "name": "@fmweb/connector",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev -p ${CONNECTOR_PORT:-3002}",
    "build": "next build",
    "start": "next start -p ${CONNECTOR_PORT:-3002}",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fmweb/shared": "workspace:*",
    "next": "^15.1.6",
    "pino": "^9.6.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
